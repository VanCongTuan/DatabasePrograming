@model IEnumerable<LTCSDLMayBay.Models.TuyenBay>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Tuyến Bay</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SanBayDen.TenSB)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SanBayDi.TenSB)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SanBayDen.TenSB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SanBayDi.TenSB)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaTuyenBay }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaTuyenBay }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaTuyenBay })
        </td>
    </tr>
}

</table>
